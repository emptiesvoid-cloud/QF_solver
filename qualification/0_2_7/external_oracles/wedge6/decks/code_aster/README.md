# WP05 Code_Aster PENTA6 deck status

The pinned Docker image is available locally and exposes Code_Aster 18.1.0.
`WP05-A-penta6-affine.comm` is a minimal deck-validation input, not a QF
correlation: QF WEDGE6 is not implemented in this baseline. The remaining
case specifications are controlled planning inputs. A future comparison must
pin modelisation, quadrature, output fields, command and raw-output digest
before it can be classified as `PASS_EXTERNAL`.
