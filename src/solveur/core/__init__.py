"""Core model, assembly, analysis and solver layers.

The subpackages expose the internal ownership boundaries used by the solver.
Legacy flat module paths remain compatibility entry points for the 0.2.x API.
"""
