# G05-B Evidence Batch

## Status

This document records the dedicated modal/dynamic/harmonic execution batch
prepared for `026-G05`. It is evidence for the gate review; it does not close
the official gate. The bounded refinement policies were Owner-approved on
2026-08-29; external evidence and final clean-worktree provenance remain
separate closeout requirements.

| Field | Value |
| --- | --- |
| Qualified source SHA | `fbae9d983da451052d95e111a85970f93899e409` |
| Worktree during execution | `dirty=false` |
| Registry | `qualification/0_2_6/g05_deep_case_registry.json` |
| Registry digest | `ebdb92cfcaa5b55e17a760042c921772b690b7f270ffd4148d1f7156b43e3db4` |
| Runner | `scripts/run_g05b.py` |
| Result manifest | `manifest.json` generated by the controlled runner |

## Executed batch

The controlled batch completed **12/12 PASS**, with zero failures, blocked
cases or expected failures:

| Family | Target in contract | Dedicated cases executed | Pass | Variations |
| --- | ---: | ---: | ---: | --- |
| MOD | 14 | 4 | 4 | one/two modes, modulus and density sensitivity |
| DYN | 16 | 4 | 4 | two time steps, damping, tabulated transient |
| HAR | 12 | 4 | 4 | off-resonance, refined grid, damping, modulus sensitivity |

These 12 cases are a deep supplemental batch. They are not silently added to
the 180-case foundation catalog, whose separate corpus contract remains
unchanged.

## Extended family coverage

The complete family campaign is recorded in
`0_2_6_g05_family_coverage.md` and its generated source of truth
`qualification/vnv/g05b_family_coverage/summary.json`. It executes MOD 14,
DYN 32 time-level cases and HAR 12 across TET4, TET10, HEX8, HEX20, BEAM2,
MITC3/MITC3+, MITC4 and discrete. Every case is run through the common
`solve_model` routes, while the resulting status remains internal
prequalification rather than an automatic qualification claim.

## Checks performed

* `python scripts/run_g05b.py --output <temporary-directory>`: 12/12 PASS;
* `python -m pytest tests/unit/test_vnv_026_framework.py tests/verification/test_dynamic_family_campaign.py -q`: 14 passed;
* `python -m ruff check scripts/run_g05b.py tests/unit/test_vnv_026_framework.py`: PASS;
* `python -m compileall -q scripts/run_g05b.py`: PASS;
* `git diff --check`: PASS;
* worktree after source commit: clean.

## Open qualification items

`G05B` execution is complete for the added 12-case batch, but `026-G05`
closeout is not recommended. The remaining items are:

1. attach the required analytical/reference and external correlations with
   complete provenance;
2. aggregate the case histories into a final gate decision without treating
   `READY` as `QUALIFIED`.

The policies in `qualification/0_2_6/g05_requirements.json` are now
`OWNER_APPROVED_BOUNDED`; their scope is limited to G05 refinement decisions.
